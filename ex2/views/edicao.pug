doctype html
html
  head
    title Eurovisão - Edição #{edicao._id}
    meta(charset="utf-8")
  body
    h1 Edição #{edicao._id}
    ul
      li ID: 
        a(href=`/${edicao._id}`) #{edicao._id}
      li Ano: #{edicao.anoEdição}
      li País Organizador: 
        a(href=`/paises/${edicao.organizacao}`) #{edicao.organizacao}
      li País Vencedor: 
        if edicao.vencedor
          a(href=`/paises/${edicao.vencedor}`) #{edicao.vencedor}
        else
          i (sem vencedor)

    h2 Músicas
    table(border="1")
      thead
        tr
          th Título
          th Intérprete
          th País
      tbody
        each musica in edicao.musicas
          tr
            td #{musica.título}
            td #{musica.intérprete}
            td #{musica.país}

    a(href="/") ⬅ Voltar à Página Principal
